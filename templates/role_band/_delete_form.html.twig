<form method="post" action="{{ path('app_role_band_delete', {'id': role_band.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role_band.id) }}">
    <button class="btn">Delete</button>
</form>
