{% extends 'base.html.twig' %}

{% block title %}MusicCategory{% endblock %}

{% block body %}
    <h1>MusicCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ music_category.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ music_category.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_music_category_index') }}">back to list</a>

    <a href="{{ path('app_music_category_edit', {'id': music_category.id}) }}">edit</a>

    {{ include('music_category/_delete_form.html.twig') }}
{% endblock %}
