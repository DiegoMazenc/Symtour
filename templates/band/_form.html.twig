{{ form_start(form) }}
{# {{ form_label(form.name, "Nom du projet *", {'label_attr': {'class': 'text-blue-400 font-semibold'}}) }} <br> #}
    {{ form_widget(form.name, {
        'attr': {
            'class': 'rounded-lg p-2 relative border-2 bg-slate-100 border-slate-400 mt-1 mb-3 w-3/5 ',
            'placeholder': 'Pays'
        }
    }) }}
    <hr class="my-5 border-1"> 

    {# {{ form_label(form.logo, null, {'label_attr': {'class': 'text-blue-400 font-semibold'}}) }} <br> #}
    {{ form_widget(form.logo, {
        'attr': {
            'class': 'rounded-lg p-2 relative border-2 bg-slate-100 border-slate-400 mt-1 mb-3 w-3/5 ',
            'placeholder': 'Pays'
        }
    }) }} <br>

    {# {{ form_label(form.music_category, "Catégorie Musicale *", {'label_attr': {'class': 'text-blue-400 font-semibold'}}) }} <br> #}
    {{ form_widget(form.music_category, {
        'attr': {
            'class': 'rounded-lg p-2 relative border-2 bg-slate-100 border-slate-400 mt-1 mb-3 w-3/5 ',
            'placeholder': 'Pays'
        }
    }) }}<br>
    
    {# {{ form_label(form.define_style, "Sous Catégorie musicale *", {'label_attr': {'class': 'text-blue-400 font-semibold'}}) }} <br> #}
    {{ form_widget(form.define_style, {
        'attr': {
            'class': 'rounded-lg p-2 relative border-2 bg-slate-100 border-slate-400 mt-1 mb-3 w-3/5 ',
            'placeholder': 'Pays'
        }
    }) }}
    <br><br>
    <button class="px-3 py-2 rounded-lg bg-blue-400 hover:bg-blue-500 text-white font-semibold">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
