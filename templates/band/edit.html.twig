{% extends 'base.html.twig' %}

{% block title %}Edit Band
{% endblock %}

{% block body %}

		<div class="user-contain bg-slate-100">
			<div class="form-edit bg-white p-10 rounded-xl shadow-lg">
			<div class="w-flex">
				{{ include('band/_form.html.twig', {'button_label': 'Update'}) }}
				<div class="flex items-center justify-center">
					<a href="{{ path('app_band_delete', {'id': band.id}) }}" class="text-center w-full  border-2 border-red-200 hover:border-red-500 hover:bg-red-300 rounded-lg p-2 duration-300" onclick="return confirm('Voulez-vous vraiment supprimer votre Projet ?')">Supprimer le Projet</a>
				</div>
			</div>
			</div>
		</div>

{% endblock %}
