{% extends 'base.html.twig' %}

{% block title %}Edit BandMember{% endblock %}

{% block body %}
    <div class="edit-band  flex bg-sky-950 ">
		<div class="nav-edit-profil basis-1/4 bg-sky-950 p-5 h-screen text-white ">
    <h1>Edit Band</h1>
    <div class="nav-contain ">
			<ul>
				{# <li class="py-5 flex"><a href="{{ path('app_band_edit', {'id': band.id }) }} ">Informations </a><img src="https://placehold.co/40" alt=""></li>
				<li class="py-5 flex"><a href="{{ path('app_band_member_edit', {'id': app.user.id}) }}" > Membres </a><img src="https://placehold.co/40" alt=""></li> #}
				{# <li>Paramètres profile</li> #}
			</ul>
    </div>
		</div>
		<div class="form-contain basis-3/4  py-20 bg bg-slate-100">
        <div class="form-edit bg-white ml-36 p-10 rounded-l-xl shadow-lg">
    <h1>Edit BandMember</h1>
    {{ include('band_member/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        </div>
	</div>


    <a href="{{ path('app_band_member_index') }}">back to list</a>

    {{ include('band_member/_delete_form.html.twig') }}
{% endblock %}
